architecture:
  hidden_layers: [128, 64, 32]
  activation: relu

ppo:
  learning_rate: 0.00001
  gamma: 0.99
  clip_epsilon: 0.1
  gae_lambda: 0.95
  epochs: 4
  mini_batch_size: 128
  rollout_steps: 1024
  entropy_coef: 0.01
  value_coef: 0.5
  max_grad_norm: 0.5
